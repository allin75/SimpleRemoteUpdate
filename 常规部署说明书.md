# 常规部署说明书（客户傻瓜版）

这份说明书只讲一件事：

怎么把“程序配置”这一页填好。

你不用懂技术细节，只要照着填就可以。

## 一、这页是干什么的

这页是给每个程序保存部署信息的。

简单理解：

- 左边：程序列表
- 右边：当前程序的配置

如果你有多个程序，就左边建多个。

## 二、左边怎么用

左边每一项就是一个程序。

- 点 `新增`：增加一个新程序
- 点某个程序：右边就会显示这个程序的配置
- 带 `默认` 标记的程序：表示每次打开页面默认选中它

如果你只有一个程序，就保留一个即可。

## 三、右边每一项怎么填

下面按页面顺序讲。

### 1. `project_id`

这是程序编号。

怎么填：

- 随便填一个不重复的英文名即可
- 最好简单一点

示例：

```text
default
web-prod
api-prod
```

### 2. `name`

这是程序名称。

怎么填：

- 填你自己看得懂的名字

示例：

```text
官网前端
订单系统后端
YourServiceName
```

### 3. `current_version`

这是当前版本号。

怎么填：

- 按 `1.0.0` 这种格式填写

示例：

```text
0.0.1
1.0.4
2.3.0
```

### 4. `service_name`

这是服务名。

你只需要这样理解：

- 如果你的程序是 Windows 服务，就填
- 如果你的程序只是一个前端页面或普通文件，就可以不填

填了以后：

- 部署时会先停服务
- 部署完成后会再启动服务

不填的话：

- 只替换文件

示例：

```text
YourServiceName
MISESmartAudit
```

### 5. `target_dir`

这是程序部署到哪个文件夹。

这个最重要，不能填错。

怎么填：

- 填程序实际运行的目录
- 一般用完整路径

示例：

```text
C:/YourApp
E:/CompalTest/SmartAuditForTest
E:/Web/site/dist
```

### 6. `max_upload_mb`

这是允许上传的最大包大小，单位是 MB。

不知道填多少时，直接填：

```text
1024
```

如果你的包很小，也可以填：

```text
200
500
```

### 7. `default_replace_mode`

这里一般只要记一句话：

- 不确定选什么，就选 `full`

说明：

- `full`：完整替换，旧文件会一起清掉
- `partial`：只替换新包里的文件，旧文件保留

客户常规使用时，建议：

```text
full
```

## 四、下面两个大框怎么填

这是最常用的两个框。

### 1. `backup_ignore_text`

这个框表示：

备份时，哪些文件不要备份。

一般直接填下面这些就够了：

```text
logs/
temp/
*.log
```

意思是：

- 日志目录不备份
- 临时目录不备份
- `.log` 文件不备份

### 2. `replace_ignore_text`

这个框表示：

部署时，哪些文件不要覆盖。

一般直接填下面这些：

```text
logs/
temp/
*.log
appsettings.json
```

为什么这样填：

- 保留日志
- 保留临时文件
- 保留配置文件

如果你的程序还有别的重要本地文件，也可以继续加进去。

比如：

```text
logs/
temp/
*.log
appsettings.json
web.config
.env
uploads/
```

## 五、最简单的填写模板

如果你不知道怎么配，直接照这个填。

```text
project_id: default
name: YourServiceName
current_version: 0.0.1
service_name: YourServiceName
target_dir: C:/YourApp
max_upload_mb: 1024
default_replace_mode: full

backup_ignore_text:
logs/
temp/
*.log

replace_ignore_text:
logs/
temp/
*.log
appsettings.json
```

## 六、3 种最常见情况

### 情况 1：后端服务

这样填：

- `service_name`：要填
- `target_dir`：填程序目录
- `default_replace_mode`：选 `full`
- `replace_ignore_text`：保留 `appsettings.json`

### 情况 2：前端页面

这样填：

- `service_name`：留空
- `target_dir`：填前端发布目录，比如 `dist`
- `default_replace_mode`：选 `full`

### 情况 3：只想更新少量文件

这样填：

- `default_replace_mode`：选 `partial`

但是要注意：

- `partial` 可能留下旧文件
- 如果更新后有问题，优先改回 `full`

## 七、最后怎么保存

全部填完后：

1. 如果想把当前程序设成默认，就勾上 `保存后设为默认程序`
2. 点击 `保存当前程序配置`

保存成功后，这个程序的配置就固定好了。

## 八、你只要记住这 4 个重点

1. `service_name` 决定要不要停服务
2. `target_dir` 决定文件发到哪里
3. `default_replace_mode` 不会选就用 `full`
4. `replace_ignore_text` 里最好加上 `appsettings.json`

如果还是不会配，就直接抄“第五部分”的模板。
